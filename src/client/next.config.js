/* eslint-disable @typescript-eslint/no-var-requires */
const dotenv = require('dotenv');
const path = require('path');

module.exports = {
  distDir: '../../.next',
  experimental: {
    appDir: true,
  },
};
